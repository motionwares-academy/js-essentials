// Hot to clone an array
const myArray = [1, 2, 3, 4, 5, 6];
const copyOfMyArray = [...myArray];

// object destructuring
const myObject = {
  name: "Bolu",
  occupation: "Developer",
  age: 100,
  likes: "food",
};

const { occupation, age, likes, name } = myObject;
// console.log(occupation);
// console.log(age);
